# S04 답안

## T1. 실행 전 예측

1. 세 양에서 합의 가중치로 사용하는 분포:

   답:

2. `H(p, q)`와 `H(p)`의 크기 비교 예측과 이유:

   답:

3. `KL(p || q)`의 부호 예측:

   답:

## T2. 세 정보량 손계산

- 각 범주의 `-log2(p_i)`:
- 각 범주의 `-log2(q_i)`:
- `H(p)`의 세 가중 항과 합:
- `H(p, q)`의 세 가중 항과 합:
- `KL(p || q)`의 세 가중 항과 합:
- 관계식 확인:

## T3. NumPy 구현 확인

- 손계산과 함수 결과의 비교:
- 사용한 배열 연산과 각 배열의 shape:

## T4. 관계식 검산

- cross-entropy 관계식의 `np.isclose` 결과:
- `KL(p || p)`의 결과와 이유:

## T5. 방향을 바꾼 실패 사례

- `q`로 가중한 cross-entropy가 나타내는 양:
- `KL(q || p)`의 예측과 실제 비교:
- shape와 합만으로 역할 오류를 찾을 수 없는 이유:

## T6. 설명 확인

답:
