# S01 답안

## T1. 실행 전 예측

1. `D`에는 관측이 몇 개 있는가?

   답:

2. `D`의 shape는 무엇이며 유일한 축은 무엇을 세는가?

   답:

3. `x_0`의 값과 shape는 무엇인가?

   답:

4. `theta`의 값을 현재 데이터에서 직접 관측했는가?

   답:

5. `observations`의 dtype은 무엇인가?

   답:

## T5. 실패 사례

각 입력에 대해 위반한 규칙과 예상되는 예외를 실행 전에 적는다.

### 1. 2차원 정수 배열

```python
np.array([[1, 0, 1, 1]], dtype=np.int64)
```

- 위반한 규칙:
- 예상 예외:
- 실행 후 확인:

### 2. 1차원 실수 배열

```python
np.array([1.0, 0.0], dtype=np.float32)
```

- 위반한 규칙:
- 예상 예외:
- 실행 후 확인:

### 3. 허용 범위를 벗어난 값을 포함한 배열

```python
np.array([1, 2], dtype=np.int64)
```

- 위반한 규칙:
- 예상 예외:
- 실행 후 확인:

## T6. 설명 확인

이 문제에서 `D`, `x_i`, `theta`는 각각 무엇이며, `D.shape == (4,)`의 두 표시는 무엇을 뜻하는가?

답:
