# `coding.python_scalar` — Python 스칼라 값

- 필요한 수준: 숫자 하나와 값 여러 개를 구분할 수 있다.
- 뜻: `3`, `1.5`처럼 축이나 원소 모음을 갖지 않는 값 하나다.
- 진단: `3`과 `[3]`이 같은 구조인지 설명한다.
- 최소 복습: `3`은 값 하나이고 `[3]`은 원소 하나를 담은 리스트다.
- 예제: NumPy에서 `np.array(3).shape == ()`, `np.array([3]).shape == (1,)`다.
