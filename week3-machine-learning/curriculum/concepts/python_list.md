# `coding.python_list` — Python 리스트

- 필요한 수준: 코드를 읽고 원소 수를 셀 수 있다.
- 뜻: `[1, 0, 1]`처럼 여러 값을 순서대로 담는 Python 객체다.
- 진단: `[3, 5, 7, 9]`의 원소 수와 첫 원소를 말해본다.
- 최소 복습: 원소 수는 `len(values)`, 첫 원소는 `values[0]`으로 확인한다.
- 예제: `values = [3, 5, 7, 9]`이면 `len(values) == 4`다.
