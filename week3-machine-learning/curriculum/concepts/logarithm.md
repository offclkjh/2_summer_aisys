# `math.logarithm` — 로그 법칙

- 필요한 수준: 곱을 로그의 합으로 바꾼다.
- 뜻: 양수에 대해 `log(ab)=log(a)+log(b)`, `log(a/b)=log(a)-log(b)`다.
- 진단: `log(p1*p2*p3)`를 합으로 쓴다.
- 최소 복습: 확률의 긴 곱을 합으로 바꾸면 underflow를 줄이고 미분하기 쉬워진다.
- 예제: `log(0.5*0.25)=log(0.5)+log(0.25)`다.
