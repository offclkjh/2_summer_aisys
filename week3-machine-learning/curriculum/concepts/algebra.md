# `math.algebra` — 식 전개와 완전제곱

- 필요한 수준: 이차식을 전개하고 제곱꼴로 묶는다.
- 뜻: `ax^2+bx+c`를 `a(x-m)^2+k` 꼴로 바꾸어 중심과 곡률을 읽는다.
- 진단: `x^2-4x`를 완전제곱한다.
- 최소 복습: `x^2-4x=(x-2)^2-4`다.
- 예제: Gaussian posterior 유도에서 `mu`에 대한 이차항을 이 방식으로 묶는다.
