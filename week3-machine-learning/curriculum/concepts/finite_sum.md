# `math.finite_sum` — 유한합

- 필요한 수준: 합 기호를 전개하고 직접 계산한다.
- 뜻: `sum(i=1..N) x_i`는 `x_1 + ... + x_N`을 줄여 쓴 것이다.
- 진단: `[2, 3, 5]`에 대해 `sum_i x_i`를 계산한다.
- 최소 복습: 합의 아래·위 첨자는 더할 index의 시작과 끝을 나타낸다.
- 예제: `2 + 3 + 5 = 10`이다.
