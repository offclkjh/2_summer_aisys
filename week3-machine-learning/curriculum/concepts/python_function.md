# `coding.python_function` — Python 함수

- 필요한 수준: 함수의 입력, 반환값, 호출을 구분한다.
- 뜻: 입력을 받아 정해진 계산을 수행하고 결과를 반환하는 코드 단위다.
- 진단: `def twice(x): return 2*x`의 입력과 반환값을 말한다.
- 최소 복습: 정의는 `def`, 호출은 `twice(3)`, 반환은 `return`으로 표현한다.
- 예제: `twice(3)`은 `6`을 반환한다.
