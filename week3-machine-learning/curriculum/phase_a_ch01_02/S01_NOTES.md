# S01 워크시트 — 객체와 shape

코드를 실행하기 전에 빈칸을 채운다. 여기서 shape는 실제 숫자와 축의 의미를 함께 쓴다.

## 1. 객체 지도

| 기호 | 이 세션에서의 의미 | shape | 관측/잠재/학습/고정/action | 근거 |
|---|---|---|---|---|
| $D$ |  |  |  |  |
| $x$ |  |  |  |  |
| $y$ |  |  |  |  |
| $z$ |  |  |  |  |
| $\theta$ |  |  |  |  |
| $\alpha$ |  |  |  |  |
| $a$ |  |  |  |  |

모든 기호가 세 사례에 반드시 등장하는가? 등장하지 않는 기호가 있다면 억지로 값을 만들지 말고 그 이유를 쓴다.

## 2. 실행 전 예측

### 동전

- 한 번의 관측과 전체 데이터 $D$를 구분해 적기:
- 코드 예제 배열의 shape, dtype, 각 축의 의미:
- 관측할 수 있는 객체와 알 수 없어서 추론해야 하는 객체:
- 일부러 잘못된 rank, dtype, 값을 하나씩 만들었을 때 기대하는 예외:

### Noisy sensor

- `features`의 세 축 `(N, T, C)`가 각각 세는 것:
- `labels`와 반드시 일치해야 하는 축과 그 이유:
- 참 상태와 센서 출력 중 관측 객체/잠재 객체 구분:
- 코드 예제에서 예상되는 `features.shape`, `labels.shape`, dtype:

### Linear layer

- $x$, $\theta$, 입력 전 activation, 출력 후 activation에 대응하는 tensor:
- `inputs`, `weight`, `bias`의 shape와 학습/고정 여부:
- 예상 출력의 shape와 그렇게 판단한 근거(수치 계산 없이):
- 세 tensor에서 dtype/device가 호환되어야 하는 이유:

## 3. NumPy에서 PyTorch로

- 변환 전후 반드시 유지할 항목:
- 호출자가 명시적으로 선택할 항목:
- shape는 같아도 의미가 틀릴 수 있는 사례 하나:

## 4. 종료 시 2분 설명 체크리스트

- [ ] $D,x,y,z,\theta,\alpha,a$를 관측·잠재·학습·고정·action으로 구분했다.
- [ ] 동전, noisy sensor, linear layer에서 각 축의 의미를 말할 수 있다.
- [ ] rank 오류와 연결 차원 불일치가 어떻게 다른지 설명할 수 있다.
- [ ] NumPy→Torch 변환에서 shape, dtype, device 계약을 설명할 수 있다.
- [ ] 유효 입력과 고의로 잘못 만든 입력으로 테스트 결과를 확인했다.
